---
phase: 01-frame-the-guide
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - AgenticAIGuide.md
  - .planning/phases/01-frame-the-guide/01-framing-contract.md
autonomous: true
requirements:
  - SCOPE-01
  - SCOPE-02
  - SCOPE-03
  - SCOPE-04
  - PRES-02
must_haves:
  truths:
    - "The guide explicitly says it is about local tool-using agents rather than agentic AI in general."
    - "The guide names Claude Code and Codex CLI as examples while keeping the explanation pattern-first."
    - "New material starts after the protected prefix and hands off from frameworks to execution environments."
    - "Later writing is constrained to operating patterns, artifacts, skills, delegation, verification, and design rather than tutorials or comparisons."
  artifacts:
    - "AgenticAIGuide.md contains an environment-pivot transition appended after the protected prefix."
    - ".planning/phases/01-frame-the-guide/01-framing-contract.md records the exact Phase 1 framing rules for later phases."
  key_links:
    - "AgenticAIGuide.md reuses the existing `# Agentic Frameworks` ending as the handoff point into the new transition text."
    - "01-framing-contract.md repeats the same scope, example, and boundary rules introduced in AgenticAIGuide.md so later phases inherit them without reinterpretation."
---

<objective>
Append the Phase 1 framing handoff after the protected draft and record a concrete editorial contract for the later writing phase.

Purpose: Turn the locked context decisions into visible guide text and a reusable writing contract without rewriting the approved earlier draft.
Output: A bounded environment-pivot addition in `AgenticAIGuide.md` and a phase-local framing contract for later execution.
</objective>

<execution_context>
@/home/nick/.codex/get-shit-done/workflows/execute-plan.md
@/home/nick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-frame-the-guide/1-CONTEXT.md
@.planning/phases/01-frame-the-guide/01-RESEARCH.md
@AgenticAIGuide.md
@.planning/phases/01-frame-the-guide/01-protected-prefix.md
@.planning/phases/01-frame-the-guide/verify-protected-boundary.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the environment-pivot framing to the guide</name>
  <files>AgenticAIGuide.md</files>
  <read_first>AgenticAIGuide.md, .planning/phases/01-frame-the-guide/01-protected-prefix.md, .planning/phases/01-frame-the-guide/verify-protected-boundary.sh, .planning/phases/01-frame-the-guide/1-CONTEXT.md, .planning/phases/01-frame-the-guide/01-RESEARCH.md</read_first>
  <action>Append new content to the end of `AgenticAIGuide.md`. Do not modify any existing line above the append point. Add exactly two new H2 sections after the current final paragraph under `# Agentic Frameworks`: `## From Frameworks To Environments` and `## What The Rest Of This Guide Covers`. In `## From Frameworks To Environments`, include these exact ideas in direct prose: `This guide is about local tool-using agents, not agentic AI in general.`; the distinctive subject is the local execution environment; the environment gives the model `filesystem access`, `shell access`, `structured tools`, and `persistent artifacts`; `Claude Code` and `Codex CLI` are concrete examples, not the subject of product documentation. In `## What The Rest Of This Guide Covers`, state that the rest of the guide will focus on `operating patterns`, `artifacts`, `skills`, `delegation`, `verification`, and `design`; state that `Claude Code` will be the deeper concrete example at least once; state that `Codex CLI` will serve as a confirming example showing the pattern generalizes; state that the guide is not a tutorial, comparison matrix, or provider survey. Keep the addition to 2 short sections only and preserve the existing technical tone.</action>
  <verify>bash .planning/phases/01-frame-the-guide/verify-protected-boundary.sh && rg -n "^## From Frameworks To Environments$|^## What The Rest Of This Guide Covers$|local tool-using agents, not agentic AI in general|filesystem access|shell access|structured tools|persistent artifacts|Claude Code|Codex CLI|operating patterns|artifacts|skills|delegation|verification|design|not a tutorial, comparison matrix, or provider survey" AgenticAIGuide.md</verify>
  <acceptance_criteria>
    - `AgenticAIGuide.md` contains the exact heading `## From Frameworks To Environments`.
    - `AgenticAIGuide.md` contains the exact heading `## What The Rest Of This Guide Covers`.
    - `AgenticAIGuide.md` contains the exact sentence `This guide is about local tool-using agents, not agentic AI in general.`
    - `AgenticAIGuide.md` contains the strings `filesystem access`, `shell access`, `structured tools`, and `persistent artifacts`.
    - `AgenticAIGuide.md` contains both `Claude Code` and `Codex CLI`.
    - `AgenticAIGuide.md` contains the exact phrase `not a tutorial, comparison matrix, or provider survey`.
    - `bash .planning/phases/01-frame-the-guide/verify-protected-boundary.sh` exits 0 after the append.
  </acceptance_criteria>
  <done>The guide now pivots from frameworks to local execution environments without altering the protected draft.</done>
</task>

<task type="auto">
  <name>Task 2: Record the locked framing contract for later phases</name>
  <files>.planning/phases/01-frame-the-guide/01-framing-contract.md</files>
  <read_first>.planning/phases/01-frame-the-guide/1-CONTEXT.md, .planning/phases/01-frame-the-guide/01-RESEARCH.md, AgenticAIGuide.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `.planning/phases/01-frame-the-guide/01-framing-contract.md` with these exact top-level headings in order: `# Phase 1 Framing Contract`, `## Scope Statement`, `## Example Strategy`, `## Protected Boundary`, `## Phase 2 Targets`, and `## Explicit Exclusions`. Under `## Scope Statement`, include the exact sentence `The guide is about local tool-using agents operating inside local execution environments, not agentic AI in general.` Under `## Example Strategy`, state that `Claude Code` and `Codex CLI` are pattern examples, that `Claude Code` gets one deeper concrete example, and that the document must not become product documentation. Under `## Protected Boundary`, state that everything captured in `01-protected-prefix.md` is frozen and that new writing starts after that prefix. Under `## Phase 2 Targets`, list exactly these bullet items: `terminal-native agent environments`, `core operating patterns`, `artifacts as control surfaces`, `skills and reusable workflows`, `delegation and parallel subagents`, `verification, recovery, and failure modes`, `how to design with these patterns`. Under `## Explicit Exclusions`, list exactly these bullet items: `step-by-step build tutorial`, `framework shopping`, `provider or model comparison`, `broad agentic AI theory not directly needed for local agent environments`.</action>
  <verify>rg -n "^# Phase 1 Framing Contract$|^## Scope Statement$|^## Example Strategy$|^## Protected Boundary$|^## Phase 2 Targets$|^## Explicit Exclusions$|The guide is about local tool-using agents operating inside local execution environments, not agentic AI in general\\.|Claude Code|Codex CLI|01-protected-prefix.md|terminal-native agent environments|core operating patterns|artifacts as control surfaces|skills and reusable workflows|delegation and parallel subagents|verification, recovery, and failure modes|how to design with these patterns|step-by-step build tutorial|framework shopping|provider or model comparison|broad agentic AI theory not directly needed for local agent environments" .planning/phases/01-frame-the-guide/01-framing-contract.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-frame-the-guide/01-framing-contract.md` contains the exact heading `# Phase 1 Framing Contract`.
    - `.planning/phases/01-frame-the-guide/01-framing-contract.md` contains the exact sentence `The guide is about local tool-using agents operating inside local execution environments, not agentic AI in general.`
    - `.planning/phases/01-frame-the-guide/01-framing-contract.md` contains `Claude Code` and `Codex CLI`.
    - `.planning/phases/01-frame-the-guide/01-framing-contract.md` contains `01-protected-prefix.md`.
    - `.planning/phases/01-frame-the-guide/01-framing-contract.md` contains all seven Phase 2 target bullet items exactly as written in the action.
    - `.planning/phases/01-frame-the-guide/01-framing-contract.md` contains all four exclusion bullet items exactly as written in the action.
  </acceptance_criteria>
  <done>Later phases have an exact editorial contract that preserves the Phase 1 decisions without reopening scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash .planning/phases/01-frame-the-guide/verify-protected-boundary.sh`
- [ ] `rg -n "^## From Frameworks To Environments$|^## What The Rest Of This Guide Covers$|local tool-using agents, not agentic AI in general|Claude Code|Codex CLI" AgenticAIGuide.md`
- [ ] `rg -n "^# Phase 1 Framing Contract$|^## Scope Statement$|^## Example Strategy$|^## Protected Boundary$|^## Phase 2 Targets$|^## Explicit Exclusions$" .planning/phases/01-frame-the-guide/01-framing-contract.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The guide has an explicit environment-pivot handoff after the protected prefix
- The Phase 1 framing decisions are recorded in a reusable contract for Phase 2
- The document stays bounded to two new framing sections rather than expanding into a handbook
</success_criteria>

<output>
After completion, create `.planning/phases/01-frame-the-guide/01-02-SUMMARY.md`
</output>
