---
phase: 01-frame-the-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-frame-the-guide/01-protected-prefix.md
  - .planning/phases/01-frame-the-guide/01-protected-prefix.sha256
  - .planning/phases/01-frame-the-guide/verify-protected-boundary.sh
autonomous: true
requirements:
  - PRES-01
must_haves:
  truths:
    - "The exact current text of AgenticAIGuide.md is frozen as the protected Phase 1 prefix before any new writing starts."
    - "A single command can fail fast if any line in the protected prefix changes."
  artifacts:
    - ".planning/phases/01-frame-the-guide/01-protected-prefix.md stores the current protected prefix verbatim."
    - ".planning/phases/01-frame-the-guide/01-protected-prefix.sha256 stores the checksum for the protected prefix snapshot."
    - ".planning/phases/01-frame-the-guide/verify-protected-boundary.sh verifies the current guide prefix against the stored snapshot."
  key_links:
    - "verify-protected-boundary.sh reads AgenticAIGuide.md and compares its first N lines to 01-protected-prefix.md, where N is the snapshot line count."
    - "verify-protected-boundary.sh prints a success message when the prefix matches and exits non-zero with a diff when it does not."
---

<objective>
Freeze the currently approved draft as a protected prefix and add a repeatable verification command before any Phase 1 writing touches the guide.

Purpose: Enforce the hard rule that everything currently in `AgenticAIGuide.md` remains unchanged unless the user explicitly approves edits upstream.
Output: A verbatim protected-prefix snapshot, a checksum file, and a shell verifier that future plans can run before and after guide edits.
</objective>

<execution_context>
@/home/nick/.codex/get-shit-done/workflows/execute-plan.md
@/home/nick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-frame-the-guide/1-CONTEXT.md
@.planning/phases/01-frame-the-guide/01-RESEARCH.md
@AgenticAIGuide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the approved guide prefix as a phase artifact</name>
  <files>.planning/phases/01-frame-the-guide/01-protected-prefix.md, .planning/phases/01-frame-the-guide/01-protected-prefix.sha256</files>
  <read_first>AgenticAIGuide.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-frame-the-guide/1-CONTEXT.md</read_first>
  <action>Create `.planning/phases/01-frame-the-guide/01-protected-prefix.md` as a verbatim copy of the current `AgenticAIGuide.md` file from line 1 through its current last line. Do not normalize whitespace, reorder bullets, or rewrap paragraphs. Then create `.planning/phases/01-frame-the-guide/01-protected-prefix.sha256` with a single SHA-256 checksum line for `01-protected-prefix.md` in standard `sha256sum` output format. This snapshot is the protected prefix baseline for Phase 1 because the current guide ends at `# Agentic Frameworks` and all new writing must be appended after this exact prefix.</action>
  <verify>test -f .planning/phases/01-frame-the-guide/01-protected-prefix.md && test -f .planning/phases/01-frame-the-guide/01-protected-prefix.sha256 && cmp -s AgenticAIGuide.md .planning/phases/01-frame-the-guide/01-protected-prefix.md && sha256sum -c .planning/phases/01-frame-the-guide/01-protected-prefix.sha256</verify>
  <acceptance_criteria>
    - `.planning/phases/01-frame-the-guide/01-protected-prefix.md` exists and `cmp -s AgenticAIGuide.md .planning/phases/01-frame-the-guide/01-protected-prefix.md` exits 0 before later plans run.
    - `.planning/phases/01-frame-the-guide/01-protected-prefix.md` contains the exact heading `# Agentic Frameworks`.
    - `.planning/phases/01-frame-the-guide/01-protected-prefix.sha256` contains `01-protected-prefix.md` on its only checksum line.
  </acceptance_criteria>
  <done>The current guide text is frozen as a reproducible baseline artifact and its checksum validates successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Add a protected-boundary verification command for later plans</name>
  <files>.planning/phases/01-frame-the-guide/verify-protected-boundary.sh</files>
  <read_first>.planning/phases/01-frame-the-guide/01-protected-prefix.md, .planning/phases/01-frame-the-guide/01-protected-prefix.sha256, AgenticAIGuide.md, .planning/phases/01-frame-the-guide/01-RESEARCH.md</read_first>
  <action>Create `.planning/phases/01-frame-the-guide/verify-protected-boundary.sh` as an executable POSIX shell script. The script must: 1) set `SNAPSHOT=.planning/phases/01-frame-the-guide/01-protected-prefix.md`; 2) compute `LINE_COUNT` from the snapshot with `wc -l`; 3) extract the first `LINE_COUNT` lines from `AgenticAIGuide.md` into a temporary file; 4) run `diff -u "$SNAPSHOT" "$TMP_FILE"` and exit non-zero if the prefix differs; 5) run `sha256sum -c .planning/phases/01-frame-the-guide/01-protected-prefix.sha256`; 6) print the exact success line `Protected boundary OK: AgenticAIGuide.md prefix matches Phase 1 snapshot.` when both checks pass. Use `trap` to remove the temp file on exit.</action>
  <verify>bash .planning/phases/01-frame-the-guide/verify-protected-boundary.sh</verify>
  <acceptance_criteria>
    - `.planning/phases/01-frame-the-guide/verify-protected-boundary.sh` contains the string `Protected boundary OK: AgenticAIGuide.md prefix matches Phase 1 snapshot.`
    - `.planning/phases/01-frame-the-guide/verify-protected-boundary.sh` contains the string `sha256sum -c .planning/phases/01-frame-the-guide/01-protected-prefix.sha256`.
    - `bash .planning/phases/01-frame-the-guide/verify-protected-boundary.sh` exits 0 against the unmodified baseline.
  </acceptance_criteria>
  <done>A single command now proves the protected guide prefix is unchanged and fails with a diff if it is not.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash .planning/phases/01-frame-the-guide/verify-protected-boundary.sh`
- [ ] `cmp -s AgenticAIGuide.md .planning/phases/01-frame-the-guide/01-protected-prefix.md`
- [ ] `sha256sum -c .planning/phases/01-frame-the-guide/01-protected-prefix.sha256`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 1 has a durable protected-prefix artifact and verification command
- Future plans can detect any unauthorized edit to the original draft immediately
</success_criteria>

<output>
After completion, create `.planning/phases/01-frame-the-guide/01-01-SUMMARY.md`
</output>
