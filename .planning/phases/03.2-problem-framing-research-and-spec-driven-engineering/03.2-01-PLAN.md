---
phase: 03.2-problem-framing-research-and-spec-driven-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AgenticEngineeringGuide.md
autonomous: true
requirements:
  - 03.2-REQ-01
  - 03.2-REQ-02
  - 03.2-REQ-03
  - 03.2-REQ-04
  - 03.2-REQ-05
  - 03.2-REQ-06
must_haves:
  truths:
    - "The guide's `# VI. Spec-Driven Engineering` section contains substantive content instead of a placeholder bridge."
    - "A reader can follow one coherent chain from problem framing through research and workflow design into explicit specs."
    - "Specs are presented as the execution contract that controls scope, assumptions, artifacts, and verification."
    - "The section stays general to terminal-native local-agent engineering and hands off cleanly into `# VII. GSD As A Workflow Framework`."
  artifacts:
    - path: "AgenticEngineeringGuide.md"
      provides: "Completed `# VI. Spec-Driven Engineering` section with compact subsection structure, specs-first emphasis, and a clean `# VI -> # VII` bridge"
      min_lines: 640
  key_links:
    - from: "AgenticEngineeringGuide.md"
      to: "AgenticEngineeringGuide.md"
      via: "`# VI` internal chain from framing -> research -> workflow design -> specs"
      pattern: "^## Framing Decides What Work Exists$|^## Research Changes What The Agent Should Believe$|^## Workflow Design Determines How Work Moves$|^## Specs Are The Execution Contract$"
    - from: "AgenticEngineeringGuide.md"
      to: "AgenticEngineeringGuide.md"
      via: "closing paragraph of `# VI` hands off to `# VII` as a framework-layer realization"
      pattern: "framework|workflow structure|GSD"
---

<objective>
Replace the reserved `# VI. Spec-Driven Engineering` bridge with the actual section this phase owns.

Purpose: Teach spec-driven engineering as the workflow-design layer that turns local-agent capability into bounded, verifiable engineering work, without drifting into tutorial or framework-internal detail.
Output: An updated `AgenticEngineeringGuide.md` whose `# VI` section is substantive, compact, specs-centered, and positioned as the conceptual bridge into the existing GSD section.
</objective>

<execution_context>
@/home/nick/.codex/get-shit-done/workflows/execute-plan.md
@/home/nick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-add-the-new-core-sections/2-CONTEXT.md
@.planning/phases/02.1-add-gsd-framework-example-and-skill-reference/02.1-CONTEXT.md
@.planning/phases/03.1-document-architecture-and-flow/03.1-CONTEXT.md
@.planning/phases/03.1-document-architecture-and-flow/03.1-02-SUMMARY.md
@.planning/phases/03.1-document-architecture-and-flow/03.1-stabilized-prefix.md
@.planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh
@.planning/phases/03.2-problem-framing-research-and-spec-driven-engineering/03.2-CONTEXT.md
@.planning/phases/03.2-problem-framing-research-and-spec-driven-engineering/03.2-RESEARCH.md
@.planning/phases/03.2-problem-framing-research-and-spec-driven-engineering/03.2-VALIDATION.md
@scripts/update-guide-toc.mjs
@AgenticEngineeringGuide.md

<interfaces>
Current placeholder section to replace:
```markdown
# VI. Spec-Driven Engineering

Sections `IV` and `V` establish the environment and then one concrete harness. The next layer is the workflow-design question: how good work gets framed, researched, scoped, and turned into explicit specs before execution starts.

...

For Phase `03.1`, this section stays a reserved bridge.
```

Locked target shape for this phase:
```text
# VI. Spec-Driven Engineering
## Framing Decides What Work Exists
## Research Changes What The Agent Should Believe
## Workflow Design Determines How Work Moves
## Specs Are The Execution Contract
```

Locked exclusions:
```text
- no detailed spec-writing checklist appendix
- no broader framework comparisons
- no GSD command names or workflow internals inside # VI
- no edits before # VI unless the boundary verifier is intentionally redefined, which this phase must not do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace the reserved `# VI` bridge with substantive specs-centered section content</name>
  <files>AgenticEngineeringGuide.md</files>
  <read_first>
AgenticEngineeringGuide.md
.planning/phases/03.2-problem-framing-research-and-spec-driven-engineering/03.2-CONTEXT.md
.planning/phases/03.2-problem-framing-research-and-spec-driven-engineering/03.2-RESEARCH.md
.planning/phases/03.1-document-architecture-and-flow/03.1-02-SUMMARY.md
.planning/phases/03.1-document-architecture-and-flow/03.1-stabilized-prefix.md
  </read_first>
  <acceptance_criteria>
- `AgenticEngineeringGuide.md` contains the exact headings `# VI. Spec-Driven Engineering`, `## Framing Decides What Work Exists`, `## Research Changes What The Agent Should Believe`, `## Workflow Design Determines How Work Moves`, and `## Specs Are The Execution Contract`.
- The placeholder sentences `Phase \`03.2\` will add the substantive material` and `For Phase \`03.1\`, this section stays a reserved bridge.` no longer appear in `AgenticEngineeringGuide.md`.
- The `# VI` section contains the exact phrase `A spec is the execution contract`.
- The `# VI` section contains the exact words `scope`, `assumptions`, `artifacts`, and `verification`.
  </acceptance_criteria>
  <action>Replace the entire existing `# VI. Spec-Driven Engineering` body in `AgenticEngineeringGuide.md` with a compact four-subsection section that reads as one argument. Use these exact subsection headings in this order: `## Framing Decides What Work Exists`, `## Research Changes What The Agent Should Believe`, `## Workflow Design Determines How Work Moves`, and `## Specs Are The Execution Contract`. Keep the section mostly conceptual, with at most one short contrastive example. Make the prose explicitly teach this chain: problem framing decides the real target and boundaries of the work; research corrects assumptions with evidence from the actual codebase, docs, or environment; workflow design decides sequencing, decomposition, checkpoints, artifacts, and verification loops; specs turn those upstream decisions into an execution contract. In the final subsection, include the exact sentence `A spec is the execution contract.` Then explain in plain prose that the contract names what is in scope, what assumptions are being made, which artifacts should exist, what evidence counts, and what done means. Give the specs subsection the most space and sharpest wording in the section so it is visibly the center of gravity. Remove all placeholder bridge language from the old `# VI` body.</action>
  <verify>
    <automated>awk '/^# VI\. Spec-Driven Engineering$/{flag=1;print;next}/^# VII\. GSD As A Workflow Framework$/{flag=0}flag' AgenticEngineeringGuide.md | rg -n '^# VI\. Spec-Driven Engineering$|^## Framing Decides What Work Exists$|^## Research Changes What The Agent Should Believe$|^## Workflow Design Determines How Work Moves$|^## Specs Are The Execution Contract$|A spec is the execution contract\.|scope|assumptions|artifacts|verification' && ! rg -n 'Phase `03\.2` will add the substantive material|For Phase `03\.1`, this section stays a reserved bridge\.' AgenticEngineeringGuide.md</automated>
  </verify>
  <done>`# VI` is now substantive, specs-centered, and structured as a single chain from framing through research and workflow design into explicit execution contracts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tighten the `# VI -> # VII` bridge and run the docs guardrails</name>
  <files>AgenticEngineeringGuide.md</files>
  <read_first>
AgenticEngineeringGuide.md
.planning/phases/03.2-problem-framing-research-and-spec-driven-engineering/03.2-CONTEXT.md
.planning/phases/03.2-problem-framing-research-and-spec-driven-engineering/03.2-VALIDATION.md
.planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh
scripts/update-guide-toc.mjs
  </read_first>
  <acceptance_criteria>
- The paragraph immediately before `# VII. GSD As A Workflow Framework` contains the exact string `framework`.
- No line inside `# VI` contains the strings `gsd-`, `gsd_`, `skill reference`, or `lifecycle`.
- `bash .planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh` exits successfully after the edits.
- `node scripts/update-guide-toc.mjs` runs successfully after the edits.
  </acceptance_criteria>
  <action>Revise the closing paragraph of `# VI` so it makes one explicit layered handoff into `# VII`: frameworks package explicit contracts, workflow structure, and reusable artifacts on top of the local-agent environment, and GSD is one concrete example of that layer. Keep the mention of GSD to this handoff boundary rather than leaking framework detail upward into the section body. After the prose is tightened, run `node scripts/update-guide-toc.mjs` so the contents block matches any new subsection headings, then run `bash .planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh` to confirm nothing before `# VI` changed. Leave the guide compact and scannable: four short subsections, sparse bullets only if truly needed, and no checklist-heavy operator prose.</action>
  <verify>
    <automated>node scripts/update-guide-toc.mjs && bash .planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh && awk '/^# VI\. Spec-Driven Engineering$/{flag=1;next}/^# VII\. GSD As A Workflow Framework$/{flag=0}flag{print}' AgenticEngineeringGuide.md | { if rg -n 'gsd-|gsd_|skill reference|lifecycle'; then exit 1; else exit 0; fi; } && sed -n '/^# VI\. Spec-Driven Engineering$/,/^# VII\. GSD As A Workflow Framework$/p' AgenticEngineeringGuide.md | tail -n 8 | rg -n 'framework|workflow structure|GSD'</automated>
  </verify>
  <done>The section ends with a clear conceptual bridge into GSD, the contents block is current, and the protected pre-`# VI` boundary still verifies cleanly.</done>
</task>

</tasks>

<verification>
Run the automated commands in both tasks, then read `# VI` straight through. Confirm that the section stays general, that the specs subsection is visibly the longest and clearest part, and that the final paragraph makes `# VII` feel like the framework-layer realization of the design logic just taught.
</verification>

<success_criteria>
1. `# VI. Spec-Driven Engineering` is no longer placeholder text and now provides substantive guidance.
2. The section teaches framing, research, workflow design, and specs as one coherent engineering chain with specs as the center of gravity.
3. The section remains compact, general, and non-tutorial, and it hands off naturally into `# VII. GSD As A Workflow Framework`.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-problem-framing-research-and-spec-driven-engineering/03.2-01-SUMMARY.md`
</output>
