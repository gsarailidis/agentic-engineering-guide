---
phase: 02-add-the-new-core-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AgenticAIGuide.md
autonomous: true
requirements:
  - STRU-01
  - STRU-02
  - STRU-03
  - STRU-04
  - STRU-05
must_haves:
  truths:
    - "The guide replaces the draft body after `# Agentic Frameworks` with a pattern-first sequence centered on the local execution environment."
    - "Readers can see how environment grounding leads to operating loops, durable artifacts, reusable skills, and delegated sub-work."
    - "Claude Code appears as the main concrete example without becoming the document spine, and Codex CLI remains a confirming example."
    - "Phase 02 ends with terminology that prepares the reader for Phase `02.1` without explaining GSD itself."
  artifacts:
    - "AgenticAIGuide.md contains the sections `## Terminal-Native Agent Environments`, `## Core Operating Patterns`, `## Artifacts As Control Surfaces`, `## Skills And Reusable Workflows`, and `## Delegation And Parallel Subagents` after the protected boundary."
    - "AgenticAIGuide.md removes or rewrites the current Claude-Code-led draft material after `# Agentic Frameworks` so the new body is the primary structure."
  key_links:
    - "`# Agentic Frameworks` flows directly into the new section spine while preserving the existing `## From Frameworks To Environments` and `## What The Rest Of This Guide Covers` transition."
    - "The artifacts section names plans, summaries, checklists, and state files as control surfaces that the later skills section can build on."
    - "The skills section defines reusable workflows in generic terms and uses Claude Code as localized illustration so Phase `02.1` can attach a concrete framework example later."
---

<objective>
Rewrite the first five Phase 2 sections so the guide teaches terminal-native local-agent patterns from environment grounding through reusable workflows and delegation.

Purpose: Replace the current product-led draft body with the agreed pattern-first conceptual spine while preserving all protected Phase 1 content.
Output: A rewritten `AgenticAIGuide.md` body after `# Agentic Frameworks` containing the environment, operating-pattern, artifact, skills, and delegation sections in a coherent order.
</objective>

<execution_context>
@/home/nick/.codex/get-shit-done/workflows/execute-plan.md
@/home/nick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-add-the-new-core-sections/2-CONTEXT.md
@.planning/phases/02-add-the-new-core-sections/02-RESEARCH.md
@.planning/phases/02-add-the-new-core-sections/02-VALIDATION.md
@.planning/phases/01-frame-the-guide/01-framing-contract.md
@.planning/phases/01-frame-the-guide/verify-protected-boundary.sh
@AgenticAIGuide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the post-framework opening with the environment, operating-pattern, and artifact sections</name>
  <files>AgenticAIGuide.md</files>
  <read_first>AgenticAIGuide.md, .planning/phases/02-add-the-new-core-sections/2-CONTEXT.md, .planning/phases/02-add-the-new-core-sections/02-RESEARCH.md, .planning/phases/01-frame-the-guide/01-framing-contract.md, .planning/phases/01-frame-the-guide/verify-protected-boundary.sh</read_first>
  <action>Rewrite the guide body after the existing Phase 1 transition sections so the first three new H2 sections are exactly `## Terminal-Native Agent Environments`, `## Core Operating Patterns`, and `## Artifacts As Control Surfaces`. Preserve every line before `# Agentic Frameworks` and keep the Phase 1 transition sections in place. Treat all later draft content as replaceable. In the environment section, define the local filesystem, shell, structured tools, permissions, and durable working state as the subject of the guide and make clear that these environments turn the model into an operator inside a real system. In the operating-patterns section, explain the recurring inspect -> plan -> act -> check -> iterate loop and ground it in real repo work rather than generic AI theory. In the artifacts section, treat plans, task files, summaries, state files, and checklists as control surfaces that shape execution, not as incidental logs. Keep Claude Code present only as a concise concrete example and use Codex CLI as a confirming example that the pattern generalizes.</action>
  <verify><automated>.planning/phases/01-frame-the-guide/verify-protected-boundary.sh && rg -n "^## Terminal-Native Agent Environments$|^## Core Operating Patterns$|^## Artifacts As Control Surfaces$|filesystem|shell|permissions|durable working state|inspect|plan|act|check|iterate|control surface|summary|state file|checklist|Claude Code|Codex CLI" AgenticAIGuide.md</automated></verify>
  <done>The rewritten body starts with the environment-to-artifacts conceptual spine and the protected prefix still verifies cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add the reusable-workflow and delegation sections with a Phase 02.1 handoff</name>
  <files>AgenticAIGuide.md</files>
  <read_first>AgenticAIGuide.md, .planning/phases/02-add-the-new-core-sections/2-CONTEXT.md, .planning/phases/02-add-the-new-core-sections/02-RESEARCH.md, .planning/phases/02-add-the-new-core-sections/02-VALIDATION.md</read_first>
  <action>Continue the rewritten body with exactly `## Skills And Reusable Workflows` and `## Delegation And Parallel Subagents`. Define skills as packaged operating patterns layered on top of the base environment and describe workflows as reusable orchestration that reduces improvisation. Use one concentrated Claude Code example or subsection here to make the abstractions concrete, but keep the section about transferable patterns rather than product documentation. In the delegation section, explain scoped sub-work, artifact-backed decomposition, bounded context, and parallel execution as explicit orchestration choices rather than magic autonomy. End one of these sections with a short conceptual bridge showing why workflow frameworks naturally emerge from skills, artifacts, and execution control, while explicitly avoiding any detailed GSD description or skill catalog.</action>
  <verify><automated>.planning/phases/01-frame-the-guide/verify-protected-boundary.sh && rg -n "^## Skills And Reusable Workflows$|^## Delegation And Parallel Subagents$|skill|workflow|reusable|delegate|delegation|subagent|parallel|scoped|artifact-backed|execution control|framework" AgenticAIGuide.md</automated></verify>
  <done>The guide now covers reusable workflows and delegation in generic pattern language, with a clean conceptual handoff to Phase `02.1` and no premature GSD deep dive.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.planning/phases/01-frame-the-guide/verify-protected-boundary.sh`
- [ ] `rg -n "^## Terminal-Native Agent Environments$|^## Core Operating Patterns$|^## Artifacts As Control Surfaces$|^## Skills And Reusable Workflows$|^## Delegation And Parallel Subagents$" AgenticAIGuide.md`
- [ ] `sed -n '/# Agentic Frameworks/,$p' AgenticAIGuide.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `AgenticAIGuide.md` now uses a pattern-first structure from environments through delegation
- The guide still preserves all content before `# Agentic Frameworks`
- Phase `02.1` has a clean conceptual setup but no detailed GSD explanation appears yet
</success_criteria>

<output>
After completion, create `.planning/phases/02-add-the-new-core-sections/02-01-SUMMARY.md`
</output>
