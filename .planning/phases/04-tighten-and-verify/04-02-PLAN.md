---
phase: 04-tighten-and-verify
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - AgenticEngineeringGuide.md
  - .planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md
  - .planning/phases/04-tighten-and-verify/04-verification-notes.md
autonomous: true
requirements:
  - OUTC-01
  - OUTC-02
  - OUTC-03
  - OUTC-04
  - EDIT-01
  - EDIT-02
  - EDIT-03
must_haves:
  truths:
    - "Each Phase 4 outcome and editorial-boundary requirement is explicitly marked pass or fail with concrete evidence from the final guide."
    - "The final guide reads from `# V` through `# VII` as one operating-environment argument, not as disconnected notes."
    - "The protected boundary remains intact while any final touch-up edits stay inside the permitted `# VI` through `# VII` body."
  artifacts:
    - path: ".planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md"
      provides: "Final requirement audit for `OUTC-01..04` and `EDIT-01..03`"
      contains: "PASS"
    - path: ".planning/phases/04-tighten-and-verify/04-verification-notes.md"
      provides: "Straight-through flow review, command outputs, and residual risks"
      contains: "# Phase 04 Verification Notes"
    - path: "AgenticEngineeringGuide.md"
      provides: "Final verified guide text after any audit-driven touch-up"
      contains: "# VII. GSD As A Workflow Framework"
  key_links:
    - from: ".planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md"
      to: ".planning/REQUIREMENTS.md"
      via: "one row per phase requirement"
      pattern: "OUTC-0[1-4]|EDIT-0[1-3]"
    - from: ".planning/phases/04-tighten-and-verify/04-verification-notes.md"
      to: "AgenticEngineeringGuide.md"
      via: "documented `# V -> # VI -> # VII` flow check"
      pattern: "V -> VI -> VII"
---

<objective>
Turn the Phase 4 edits into an explicit sign-off that the guide meets the reader outcomes and editorial boundaries.

Purpose: Close the phase with requirement-mapped evidence, a final straight-through flow review, and any last small touch-up that the audit reveals inside the permitted edit surface.
Output: A completed outcome matrix, final verification notes, and a guide that is verified against both the boundary script and the Phase 4 requirements.
</objective>

<execution_context>
@/home/nick/.codex/get-shit-done/workflows/execute-plan.md
@/home/nick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tighten-and-verify/4-CONTEXT.md
@.planning/phases/04-tighten-and-verify/04-RESEARCH.md
@.planning/phases/04-tighten-and-verify/04-VALIDATION.md
@.planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md
@.planning/phases/04-tighten-and-verify/04-edit-notes.md
@.planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the reader-outcome matrix with exact evidence and pass/fail status</name>
  <read_first>
    .planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md
    AgenticEngineeringGuide.md
    .planning/REQUIREMENTS.md
    .planning/phases/04-tighten-and-verify/04-RESEARCH.md
    .planning/phases/04-tighten-and-verify/04-VALIDATION.md
  </read_first>
  <files>.planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md, AgenticEngineeringGuide.md</files>
  <acceptance_criteria>
    - Every row for `OUTC-01`, `OUTC-02`, `OUTC-03`, `OUTC-04`, `EDIT-01`, `EDIT-02`, and `EDIT-03` in `04-reader-outcome-matrix.md` has `PASS` or `FAIL` in the `Status` column.
    - Every row cites a concrete section anchor or quoted phrase reference in the `Evidence in guide` column rather than placeholder text.
    - If any row is `FAIL` because of a problem inside `# VI` or `# VII`, `AgenticEngineeringGuide.md` is updated in the same task to remove the failure with the smallest bounded touch-up available inside the existing `# VI` through `# VII` body, and the row is re-marked `PASS` before the task closes.
    - If a row would require edits before `# VI` to become `PASS`, do not edit the protected prefix; record the blocker in the row notes and stop the plan for user review instead of violating the boundary.
    - `04-reader-outcome-matrix.md` contains no `TODO`, `TBD`, or `placeholder` text when the task is complete.
  </acceptance_criteria>
  <action>Read the final guide from `# IV. Agentic Frameworks` through `# VII. GSD As A Workflow Framework`, then fill every row in `.planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md` with exact evidence from the current draft. Use concrete anchors such as ``# IV. Agentic Frameworks` distinction paragraph`, ``## Artifacts As Control Surfaces` artifact list`, or ``## Why GSD Matters` closing explanation`, and mark each requirement `PASS` or `FAIL`. If the audit reveals a real issue inside `# VI` or `# VII`, make only the smallest required prose fix inside that existing body, rerun the boundary verifier, and then update the matrix row to `PASS`. Do not change any heading text, add new sections, or touch content before `# VI`; the audit may tighten or clarify existing `# VI` and `# VII` prose but may not expand structure or scope. If the audit reveals a problem that can only be fixed by editing frozen content before `# VI`, record that as a blocker instead of changing the protected prefix. Remove all placeholder wording before closing the task.</action>
  <verify>
    <automated>bash .planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh && rg -n '^\| (OUTC-01|OUTC-02|OUTC-03|OUTC-04|EDIT-01|EDIT-02|EDIT-03) \| .* \| (PASS|FAIL) \|' .planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md && ! rg -n 'TODO|TBD|placeholder' .planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md</automated>
  </verify>
  <done>The requirement matrix is fully populated with concrete evidence and final pass/fail status, and any audit-driven guide fix has already been applied within the permitted `# VI` through `# VII` edit surface.</done>
</task>

<task type="auto">
  <name>Task 2: Record the final flow verification and boundary sign-off</name>
  <read_first>
    .planning/phases/04-tighten-and-verify/04-verification-notes.md
    .planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md
    AgenticEngineeringGuide.md
    .planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh
    .planning/phases/04-tighten-and-verify/04-VALIDATION.md
  </read_first>
  <files>.planning/phases/04-tighten-and-verify/04-verification-notes.md, .planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md</files>
  <acceptance_criteria>
    - `04-verification-notes.md` exists and contains the exact headings `# Phase 04 Verification Notes`, `## Automated checks`, `## V -> VI -> VII flow review`, `## Scope-drift review`, and `## Residual risks`.
    - `04-verification-notes.md` records the exact command `bash .planning/phases/03.1-document-architecture-and-flow/verify-stabilized-boundary.sh` under `## Automated checks`.
    - `04-verification-notes.md` contains a sentence that explicitly says whether the `V -> VI -> VII` handoff reads as one argument.
    - `04-verification-notes.md` contains a sentence that explicitly states the scope-drift conclusion: whether the final document stayed out of tutorial, framework-shopping, and broad-theory territory.
    - `04-reader-outcome-matrix.md` contains no `FAIL` rows when the task closes.
  </acceptance_criteria>
  <action>Create `.planning/phases/04-tighten-and-verify/04-verification-notes.md` with the exact sections `Automated checks`, `V -> VI -> VII flow review`, `Scope-drift review`, and `Residual risks`. Under `Automated checks`, record the boundary verifier command and its successful outcome. Under `V -> VI -> VII flow review`, write a direct conclusion using the exact chain `V -> VI -> VII` about whether the operating-pattern section, the spec-driven section, and the GSD section now read as one operating-environment argument. Under `Scope-drift review`, explicitly state whether the final document stayed out of build-tutorial, framework-shopping, and broad agentic-theory territory. If any matrix row still says `FAIL` because of `# VI` or `# VII`, fix the guide or matrix before closing so the file ends with only `PASS` rows, using only bounded touch-ups inside the existing `# VI` through `# VII` body and leaving all content before `# VI` untouched. If a remaining failure would require edits before `# VI`, stop and surface the blocker rather than violating the boundary.</action>
  <verify>
    <automated>rg -n '^# Phase 04 Verification Notes$|^## Automated checks$|^## V -> VI -> VII flow review$|^## Scope-drift review$|^## Residual risks$' .planning/phases/04-tighten-and-verify/04-verification-notes.md && rg -n 'bash \\.planning/phases/03\\.1-document-architecture-and-flow/verify-stabilized-boundary\\.sh' .planning/phases/04-tighten-and-verify/04-verification-notes.md && rg -n 'V -> VI -> VII.*(reads as one argument|does not read as one argument)' .planning/phases/04-tighten-and-verify/04-verification-notes.md && rg -n '(stayed out of|did not become).*(tutorial|framework-shopping|broad agentic-theory)' .planning/phases/04-tighten-and-verify/04-verification-notes.md && ! rg -n '^\| .* \| .* \| FAIL \|' .planning/phases/04-tighten-and-verify/04-reader-outcome-matrix.md</automated>
    <manual>Read `04-verification-notes.md` and confirm the written flow judgment and scope-drift conclusion match the actual final guide rather than generic boilerplate.</manual>
  </verify>
  <done>Phase 4 has a written verification record covering automated checks, conceptual flow, scope discipline, and residual risks, and the requirement matrix closes cleanly with only passing rows.</done>
</task>

</tasks>

<verification>
Run the boundary verifier again after any audit-driven touch-up. Confirm the matrix ends with only `PASS` rows and that `04-verification-notes.md` explicitly records the `V -> VI -> VII` flow judgment and scope-drift review.
</verification>

<success_criteria>
The final guide is explicitly evidenced against every Phase 4 requirement, the protected boundary still passes, and the closeout notes show that the document stayed compact, pattern-first, and newcomer-usable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tighten-and-verify/04-02-SUMMARY.md`
</output>
